// PostCSS config — tells PostCSS which plugins to run on our CSS
export default {
  plugins: {
    // Tailwind CSS plugin: processes @tailwind directives and generates utility classes
    tailwindcss: {},
    // Autoprefixer: automatically adds vendor prefixes (-webkit-, -moz-, etc.) for browser compatibility
    autoprefixer: {},
  },
}
